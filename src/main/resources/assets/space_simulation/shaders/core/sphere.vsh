#version 150

// 世界球体(隐式球面)渲染顶点着色器。
// 几何:覆盖整个屏幕的 NDC 大三角形(3 顶点),直接置于远平面(z=1,w=1),
// 不经过任何相机/模型变换——顶点只负责产生片元,视线方向完全由片元着色器
// 从 NDC 逐像素精确重建,不存在立方体面插值在面边界不连续导致的
// 轮廓折痕/转动视角扭曲变形。

in vec3 Position;

void main() {
    gl_Position = vec4(Position.xy, 1.0, 1.0);
}
